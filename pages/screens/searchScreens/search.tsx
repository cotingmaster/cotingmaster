import React from 'react';
import SearchContainer from '../../../src/components/search/Search.container';

const SearchScreen = ({ route }: any) => {
  return <SearchContainer route={route} />;
};

export default SearchScreen;
