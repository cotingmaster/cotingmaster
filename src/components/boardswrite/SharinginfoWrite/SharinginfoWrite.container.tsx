import React from 'react';
import WriteContainer from '../commonWrite/Write.container';

const SharingInfoWriteContainer = () => {
  return <WriteContainer board={'SharingInfo'} />;
};

export default SharingInfoWriteContainer;
